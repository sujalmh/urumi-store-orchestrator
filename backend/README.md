# Backend (FastAPI)

FastAPI service for store provisioning, background tasks, and Kubernetes orchestration.

## Dev Setup (placeholder)

- Python 3.11+
- Create virtual environment
- Install dependencies from `requirements.txt`
- Configure env vars (DB URL, JWT secret, kubeconfig, Redis)
